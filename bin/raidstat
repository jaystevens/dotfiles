#!/bin/env python

import sys
import os
import time

while True:
    try:
        os.system('clear')
        os.system('date')
        os.system('cat /proc/mdstat')
        time.sleep(2)
    except KeyboardInterrupt:
        sys.exit(0)
